import { Author, FooterContainer } from './footer.styled';

type Props = {};

const Footer = ({}: Props) => {
    return (
        <FooterContainer>
            <Author>Made by James Standbridge</Author>
        </FooterContainer>
    );
};

export default Footer;
