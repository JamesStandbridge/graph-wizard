export interface ThemeType {
    primary: string;
    secondary: string;
    info: string;
    danger: string;
}

const theme: ThemeType = {
    primary: '#4CAF50',
    secondary: '#FFC107',
    info: '#765fff',
    danger: '#ff2b56',
};

export default theme;
